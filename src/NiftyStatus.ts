export enum ChunkStatus {
    QUEUED,
    UPLOADING,
    SUCCESSFUL,
    FAILED,
    CANCELED
}
export enum FileStatus {
    ADDED,
    PROCESSING,
    PROCESSED,
    QUEUED,
    UPLOADING,
    SUCCESSFUL,
    FAILED,
    CANCELED
}
