export enum ChunkStatus {
    QUEUED,
    UPLOADING,
    SUCCESSFUL,
    FAILED
}
export enum FileStatus {
    ADDED,
    PROCESSING,
    PROCESSED,
    QUEUED,
    UPLOADING,
    SUCCESSFUL,
    FAILED
}
